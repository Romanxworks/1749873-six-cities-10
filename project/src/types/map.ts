export type City = {
    name: string;
    location:{
        latitude: number,
        longitude: number,
        zoom: number
    }
  };

export type Point = {
    name: string;
    latitude: number;
    longitude: number;
    zoom: number
  };

